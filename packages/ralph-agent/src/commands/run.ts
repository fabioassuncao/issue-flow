import { printInfo } from '../ui/logger.js';

export async function runPipeline(
  _issue: string,
  _mode: string,
  _from?: string,
): Promise<number> {
  printInfo('run command not yet implemented');
  return 1;
}
